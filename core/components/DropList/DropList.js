import React from "react";

export default function DropList({ title, children }) {
  return (
    <div>
      <header>
        <h3>{title}</h3>
      </header>

      <div>TEST</div>
    </div>
  );
}
